var shortestCompletingWord = function(licensePlate, words) {
    let normalizedLicensePlate=licensePlate.replace(/[^a-zA-z]/g,"").toLowerCase()
  let splitStr=normalizedLicensePlate.split("").sort()
  console.log(splitStr)
 let completingWord 
 for()}
 console.log(completingWord)
    
};
shortestCompletingWord("1se  pst",["step","steps","stepple"])